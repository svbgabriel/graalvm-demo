package app;

public class Constants {

    public static final String JS_SCRIPT =
    """
    sum = (a, b) => a + b;

    function sub(a, b) {
        return a - b;
    }

    function multi(a, b) {
        return a * b;
    }

    div = (a, b) => a / b;
    """;
}
